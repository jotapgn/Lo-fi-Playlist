import styled from 'styled-components';

const Tag = styled.h2`
    display: inline-block;
    border-radius: 4px;
    font-size: 50px;
    background-color: var(--frontEnd);
    padding: 20px 18px;
    color: var(--grayLight)
`;

export default Tag;